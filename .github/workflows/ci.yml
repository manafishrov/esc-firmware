name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build ${{ matrix.target }} (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: f421
            artifact-name: AM32-F421-Linux
            artifact-path: |
              obj/*_F421_*.hex
              obj/*_F421_*.bin
          - os: windows-latest
            target: product
            artifact-name: AM32-product-Windows
            artifact-path: |
              obj/AM32_SKYSTARS_AM60_V2_F421_*.hex
              obj/AM32_SKYSTARS_AM60_V2_F421_*.bin
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Test current calibration and input settings
        if: runner.os == 'Linux'
        run: |
          python3 tests/test_current_calibration.py
          python3 tests/test_input_settings.py

      - name: Install ARM toolchain
        shell: bash
        run: make arm_sdk_install

      - name: Build target
        shell: bash
        env:
          BUILD_TARGET: ${{ matrix.target }}
        run: |
          if [[ "$RUNNER_OS" == Windows ]]; then
            tools/windows/make/bin/make -j2 "$BUILD_TARGET"
          else
            make -j"$(nproc)" "$BUILD_TARGET"
          fi

      - name: Upload firmware binaries
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ matrix.artifact-name }}
          path: ${{ matrix.artifact-path }}
          if-no-files-found: error
          retention-days: 14

  ci:
    name: ci
    if: always()
    needs:
      - build
    runs-on: ubuntu-latest
    steps:
      - name: Require all builds
        env:
          BUILD_RESULT: ${{ needs.build.result }}
        run: test "$BUILD_RESULT" = success
